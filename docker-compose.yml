services:
  gym-tracking-db:
    image: postgres
    container_name: gym-tracking-db-postgresql
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    networks:
      - gym-tracking-network
    volumes:
      - gym-tracking-db-data:/var/opt/postgresql/data
  nutrition-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: nutrition-db-mssql
    environment:
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      ACCEPT_EULA: ${ACCEPT_EULA}
    ports:
      - "1433:1433"
    networks:
      - nutrition-network
    volumes:
      - nutrition-db-data:/var/lib/mssql/data
  redis:
    image: redis:latest
    container_name: redis-cache-container
    ports:
      - "6379:6379"
    networks:
      - gym-tracking-network
      - nutrition-network
    volumes:
      - redis-data:/data
  rabbitmq:
    image: rabbitmq:4.2.2-management
    container_name: rabbitmq-message-broker
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - gym-tracking-network
      - nutrition-network
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
  identity-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: identity-db-mssql
    environment:
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      ACCEPT_EULA: ${ACCEPT_EULA}
    ports:
      - "1434:1433"
    networks:
      - identity-network
    volumes:
      - identity-db-data:/var/lib/mssql/data
  # api-gateway:
  #   build:
  #     context: .
  #     dockerfile: GymApp.Ocelot.Gateway/Dockerfile
  #   container_name: api-gateway
  #   ports:
  #     - "5000:8080"
  #   networks:
  #     - gym-tracking-network
  #     - nutrition-network
  #   depends_on:
  #     - gym-tracking-db
  #     - nutrition-db
  #     - redis
  #     - rabbitmq
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ASPNETCORE_URLS=http://+:8080
  # gym-tracking-service:
  #   build:
  #     context: .
  #     dockerfile: src/Services/GymTrackingService/GymApp.GymTrackingService.API/Dockerfile
  #   container_name: gym-tracking-service
  #   ports:
  #     - "8081:8080"
  #   networks:
  #     - gym-tracking-network
  #   depends_on:
  #     - gym-tracking-db
  #     - redis
  #     - rabbitmq
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ASPNETCORE_URLS=http://+:8080
  #     - ConnectionStrings__GymTrackingDatabase=Host=gym-tracking-db;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};
  # identity-service:
  #   build:
  #     context: .
  #     dockerfile: src/Services/IdentityService/GymApp.IdentityService.API/Dockerfile
  #   container_name: identity-service
  #   ports:
  #     - "8082:8080"
  #   networks:
  #     - identity-network
  #   depends_on:
  #     - identity-db
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ASPNETCORE_URLS=http://+:8080
  #     - ConnectionStrings__IdentityDatabase=Server=identity-db;Database=IdentityDB;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=True;
  # nutrition-service:
  #   build:
  #     context: .
  #     dockerfile: src/Services/NutritionService/GymApp.NutritionService.API/Dockerfile
  #   container_name: nutrition-service
  #   ports:
  #     - "8083:8080"
  #   networks:
  #     - nutrition-network
  #   depends_on:
  #     - nutrition-db
  #     - redis
  #     - rabbitmq
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ASPNETCORE_URLS=http://+:8080
  #     - ConnectionStrings__NutritionDatabase=Server=nutrition-db;Database=NutritionDB;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=True;

networks:
  nutrition-network:
    driver: bridge
  gym-tracking-network:
    driver: bridge
  identity-network:
    driver: bridge

volumes:
  gym-tracking-db-data:
  nutrition-db-data:
  rabbitmq-data:
  redis-data:
  identity-db-data:
